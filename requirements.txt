# crisprHAL Streamlit v2 — dependencies
#
# tensorflow-cpu was discontinued after TF 2.15; TF 2.16+ ships a unified
# package. Use tensorflow==2.19.0 — on CPU-only hardware (Streamlit Community
# Cloud) it automatically runs in CPU mode with no GPU overhead.
#
# Minimum TF version: 2.16 (required for native .keras format support via Keras 3).
# Models were trained and saved with TF 2.19; loading with any earlier TF 2.x
# that ships Keras 2 would not support the .keras format.
#
# Python 3.11 is specified in runtime.txt (TF 2.19 supports 3.9–3.12).
# Python 3.11 is used over 3.12 for better Streamlit Cloud stability.

streamlit>=1.40.0
tensorflow==2.19.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0
