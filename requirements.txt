# crisprHAL Streamlit v2 — dependencies
#
# IMPORTANT — Python version:
#   Streamlit Community Cloud ignores runtime.txt. The Python version MUST be
#   set to 3.13 in the app's Advanced Settings on share.streamlit.io.
#   TensorFlow has no wheels for Python 3.14 (the current Cloud default).
#
# TensorFlow 2.21.0 is the minimum version with Python 3.13 wheel support.
# Models saved with TF 2.19 (.keras / .h5) load correctly under TF 2.21 —
# TF maintains backward compatibility for saved model formats across minor versions.
#
# Minimum TF version for .keras format: 2.16 (requires Keras 3, bundled with TF 2.16+).

streamlit>=1.40.0
tensorflow==2.21.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0
